#include <iostream>

using namespace std;

int main()
{
    // ()
    // * and /
    // + and -

    double x = 10;
    double y = 5;
    double z = (x + 10) / (y * 3);
    cout << z << endl;
    return 0;
}